// Intentionally empty — diagnostic file used during Phase 2 development;
// kept around so the parity workflow doesn't need to recreate it if
// something regresses. Real parity asserts live in `mcts_parity.rs`.
